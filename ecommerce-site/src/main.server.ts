// import { bootstrapApplication } from '@angular/platform-browser';
// import { AppComponent } from './app/app.component';
// import { config } from './app/app.config.server';

// /**
//  * Bootstrap the Angular Application for Server-Side Rendering (SSR).
//  * This function initializes the Angular application and configures it with the provided server-side config.
//  */
// const bootstrap = async () => {
//   try {
//     console.log('Bootstrapping Angular application for SSR...');
    
//     // Bootstrap the Angular app for SSR using the configuration
//     await bootstrapApplication(AppComponent, config);

//     console.log('Angular application bootstrapped successfully for SSR.');
//   } catch (err) {
//     console.error('Error during Angular SSR bootstrap:', err);
//     // Handle any bootstrap errors (logging or fallback behavior)
//   }
// };

// export default bootstrap;
